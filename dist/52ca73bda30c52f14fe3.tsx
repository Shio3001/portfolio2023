import * as React from 'react';
var useContext = React.useContext, useReducer = React.useReducer, createContext = React.createContext;
var Background = function () {
    return React.createElement("div", { className: 'myself-background-div' });
};
export default Background;
